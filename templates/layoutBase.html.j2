<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}{% endblock %} -Violation Message</title>
    <style>
        .top-row {
            background-color: #333;
            color: white;
            padding: 10px 20px;
            display: flex;
            justify-content: flex-end;
            align-items: center;
        }
        .logout-tab {
            background-color: #f44336;
            color: white;
            border: none;
            padding: 8px 16px;
            cursor: pointer;
            border-radius: 4px;
            font-size: 14px;
            transition: background-color 0.3s;
        }
        .logout-tab:hover {
            background-color: #d32f2f;
        }
        h1 {
            color: #333;
        }
    </style>
</head>
<body>
    <div class="top-row">
        {% if current_user.is_authenticated %}
            <a href="{{ url_for('portals') }}"><button class="btn btn-xs btn-info ms-2">Home</button></a>
            <a href="{{ url_for('violation') }}"><button class="btn btn-xs btn-info ms-2">Violation</button></a>
            <a href="{{ url_for('atc') }}"><button class="btn btn-xs btn-info ms-2">ATC</button></a>
            <a href="{{ url_for('voltViolation') }}"><button class="btn btn-xs btn-info ms-2">Voltage Violation</button></a>
        {% endif %}
        <a href="{{ url_for('oauth.logout') }}"><button class="logout-tab ms-2">Logout</button></a>
    </div>
    <div class="content">
        {% block content %}{% endblock %}
    </div>
    {% block scripts %}{% endblock %}
</body>
</html>